version: '3.8'
services:
  db:
    image: postgres:15
    container_name: banking_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: core_banking
    ports:
      - "5432:5432"
